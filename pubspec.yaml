name: it_feels_provider_backend
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.2

dependencies:
  crypto: ^3.0.7
  http: ^1.6.0
  shelf: ^1.4.2
  shelf_router: ^1.1.2
  youtube_explode_dart: ^3.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
